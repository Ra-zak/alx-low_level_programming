#include "main.h"
/**
 * _strlen - entry for string length
 * @s: sring to be measured
 * Return: i if success
*/
int _strlen(char *s)
{
	int i;
	char p = *s

	for (i = 0; p[i] != '\0'; ++i)
	{
		return (i);
	}
}
