#include "main.h"
#include <stdio.h>
/**
 * main - prints program name
 * @argc: counts number of argument
 * @argv: array of string of argc
 * Return: 0 if success 
*/
int main(int argc, char *argv[])
{
	printf("%s \n", argv[0]);
	return (0);
}
