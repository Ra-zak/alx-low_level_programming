#include "main.h"
#include <stdio.h>
/**
 * main - prints argc
 * @argc: number of arguments passed to program
 * @argv: array conting argc
 * Return: 0 if success
*/
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);
	return (0);
}
